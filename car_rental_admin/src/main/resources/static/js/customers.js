// Làm mới table
function refreshApplications() {
    window.location.reload(); // hoặc gọi ajax nếu muốn load động
}

function changeEntriesPerPage() {
    // This is handled by the DataTable class
}